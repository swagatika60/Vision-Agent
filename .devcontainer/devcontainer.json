{
    "name": "VisionMate Build",
    "image": "mcr.microsoft.com/devcontainers/python:3.12",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgl1 libglib2.0-0 && pip install -r requirements.txt"
}
